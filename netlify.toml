# Build settings for a Git-connected deploy. Headers and redirects live in public/_headers
# and public/_redirects, so they also apply when the dist folder is deployed by drag and drop.

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "24"
